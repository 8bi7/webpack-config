import "./styles.scss";
import "./style.css";

// eslint-disable-next-line no-undef
alert("hello"); // eslint-disable-line no-alert

const fancyFunc = () => [1, 2];

const [, b] = fancyFunc();
console.log(b);
